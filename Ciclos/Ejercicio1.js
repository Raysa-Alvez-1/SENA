// Iterar desde 1 hasta 100
for (let i = 1; i <= 100; i++) {
    // Verificar si el número es un múltiplo de 3
    if (i % 3 === 0) {
        // Imprimir el número si es múltiplo de 3
        console.log(i);
    }
}
